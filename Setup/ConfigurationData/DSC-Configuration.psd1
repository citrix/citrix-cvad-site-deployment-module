@{
    AllNodes = @(
        @{
            NodeName                    = "localhost"
            PSDscAllowDomainUser        = $true
            PSDscAllowPlainTextPassword = $true
        }
    )
}
